# Disney Quiz App - Environment Variables
# Copy to .env.local for local development

# Default webhook URL (optional - can also be set via URL param)
VITE_WEBHOOK_URL=

# Default agent name (optional - can also be set via URL param)
VITE_AGENT_NAME=

# Analytics (future use)
VITE_GA_ID=
